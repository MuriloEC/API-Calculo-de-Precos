# Backend FastAPI

## Requisitos

- Python 3.11+
- Pipenv

## Instalação

Instalar dependências:

```bash
pipenv install
```

Ativar ambiente:

```bash
pipenv shell
```

Rodar servidor:

```bash
uvicorn app.main:app --reload
```